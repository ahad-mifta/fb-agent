CREATE EXTENSION IF NOT EXISTS vector;

-- Use this file as the baseline SQL if you prefer manual migrations.
-- The Prisma schema defines a vector(1536) column for OpenAI text-embedding-3-small.
-- Recommended:
--   npx prisma migrate dev --name init
--   npx prisma generate
